generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model sample {
  user_id  Int      @id @default(autoincrement())
  email    String   @unique
  role     String
}
